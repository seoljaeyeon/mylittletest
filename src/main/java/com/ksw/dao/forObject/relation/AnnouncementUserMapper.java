package com.ksw.dao.forObject.relation;

import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface AnnouncementUserMapper {

	
	/*
	 * @Insert("" + "Insert INTO Ann~") Announcement insert()
	 */
}
